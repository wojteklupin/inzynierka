def place_element_in_grid(enforced_row):
    shift = 0
    shift_all = 0
    .
    .
    .
    if split:
        shift_all = ...
        for:
            enforced_row += shift 
            shift += place_element_in_grid(enforced_row)
    return shift + shift_all
